{
  "name": "intelligent-travel-planner",
  "private": true,
  "type": "module",
  "workspaces": ["client", "server", "ml/inference-node"],
  "scripts": {
    "dev": "concurrently -n client,server \"npm:dev --workspace client\" \"npm:dev --workspace server\"",
    "lint": "npm run lint --workspace client && npm run lint --workspace server",
    "test": "npm test --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
  
}
